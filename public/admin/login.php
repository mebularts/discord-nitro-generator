<?php session_start(); require_once __DIR__.'/../../app/helpers.php'; if(is_post()){ csrf_check(); $u=$_POST['u']??''; $p=$_POST['p']??''; $row=q($pdo,'SELECT * FROM admins WHERE username=?',[$u])->fetch(); if($row && password_verify($p,$row['password'])){ $_SESSION['admin']=['id'=>$row['id']]; redirect('/admin/'); } $e='Hatalı'; } echo '<form method=post>'.csrf_field().'<input name=u><input name=p type=password><button>Giriş</button>'.(!empty($e)?$e:'').'</form>';
